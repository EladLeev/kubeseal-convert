run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - goimports
    - govet
    - staticcheck
    - durationcheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  sort-results: true